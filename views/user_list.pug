extends layout

block content
    h1= title

    ul
    each user in userList
        li
            a(href=user.url) Read more - #{user.name}
            |  (#{user.dob_yyyy_mm_dd})

    else
        li There are no users.