extends layout

block content
    h1= title

    ul
        each post in postList
            li
                
                a(href=post.url) #{post.title}
                |   - #{post.user.name} 
                |   : #{post.created_formatted}
            
        else
            li There are no posts.